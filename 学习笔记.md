怎么打开main.c 中的函数
restart
*init_ramdisk 主存中化出一部分空间充当硬盘 放er文件 可执行文件*
*真正放在主存中的不是er文件 而是其中一部分*![note1](D:\cs学习资源\计算机系统实践\note\note1.png)

按字节连址 8位



PA01:

restart  模拟计算机启动

初始化工作：

1）初始化ramdisk

2）读入入口代码entry

3）设置%eip的初值

4）初始化DRAM的模拟



1）初始化ramdisk

因为NEMU没有磁盘 -> 将内存开始位置附近一段区间作为磁盘使用 称作ramdisk

ramdisk中存放要在NEMU中允许的程序的可执行文件 而init_randisk 就是将这个文件读入
**FILE *fp = fopen(exec_file, "rb"); 
file类型指针变量fp 
该语句意思：打开名为exec_file文件 以只读二进制模式打开 且将打开文件句柄赋值给变量fp**
fread/fwrite进行读写操作
>**关于文件句柄**
 当你在程序中打开一个文件时,操作系统会分配一个唯一的标识符来代表这个文件,这个标识符就是文件句柄。
文件句柄通常是一个整数值,用于在程序中引用和操作打开的文件。不同的操作系统可能使用不同的数据类型表示文件句柄,但基本原理是一样的。
你可以使用文件句柄来执行各种文件操作,如读取、写入、定位文件指针等。这些操作都需要提供对应的文件句柄作为参数。
在 C 语言中,标准库提供了 FILE* 类型来表示文件句柄。当你调用 fopen() 函数打开一个文件时,它会返回一个 FILE* 指针,这个指针就是文件句柄。

***
2）读入入口代码entry

![image-20240827222957830](C:\Users\yiyiyyi\AppData\Roaming\Typora\typora-user-images\image-20240827222957830.png)

这个是读入文件（数据的标准格式） 

cpu.eip=ENTRY_START（设置为100000）

说明拿到的第一条地址就在0x100000这里

eip%是存放指令的寄存器

因此在真实的计算机系统中，计算机启动后首先会把

控制权交给 BIOS，BIOS 经过一系列初始化工作之后，再从磁盘中将有意义的程序读入内存中执行。

这里进行简化 把entry读入懂啊一个固定的内存位置 且将这个内存位置作为%eip的初始值

![note1](D:\cs学习资源\计算机系统实践\note\note1.png)

0-0x100000 ramdisk

0x100000 开始存放entry **entry中内容就是存放要在nemu中运行程序 模拟存储里开始**

(3)初始化主存 init_ddr3 在nemu中就是大数组

cpu_exec()  模拟cpu的工作方式 不断执行指令
exe()函数 cpu执行一条指令 已执行指令输出到日志文件log.txt中
在 nemu/src/cpu/exec/exec.c 文件中的 opcode_table 数组 **你可以看到框架代码中都已经实现了哪些指令**
nemu 退出指令执行循环：
1）达到要求循环次数
2）用户程序执行nemu_trap指令 
退出 cpu_exec()之后，NEMU 将返回到 ui_mainloop()，等待用户输入命令。
但为了再次运行程序，你需要退出 NEMU，然后重新运行。
**在 cmd_c()函数中, 调用 cpu_exec()的时候传入了参数-1 , 你知道为什么吗?**
**回答：volatile uint32_t表示一个32位无符号整数变量  -1十六进制ffffffff 表示为无符号整数 传入的数为要执行多少步 这里就是最大的无符号数 无穷步  一直执行**



对调试有用的宏！nemu/include/debug.h中
◆ Log()是 printf()的升级版，专门用来输出调试信息，同时还会输出使用
Log()所在的源文件，行号和函数，当输出的调试信息过多的时候，可以
很方便地定位到代码中的相关位置。
◆ Assert()是 assert()的升级版，当测试条件为假时, 在 assertion fail 之
前可以输出一些信息。
◆ panic()用于输出信息并结束程序，相当于无条件的 assertion fail。
代码中已经给出了这三个宏的例子，如果你不知道如何使用它们，阅读源代码
访问模仿的内存： 用swaddr_read() & swaddr_write()
swaddr 虚拟地址 
inaddr 线性地址
hwaddr 物理地址
**什么是线口一致性？**

寄存器结构体 
nemu中 通用寄存器 

questions:
1.在命令行中敲 可以传入信息 这是啥意思
2.readline("(nemu)")为什么可以实现回溯 保存记录的功能

3.<0是什么意思 说明错误？

4.char *cmd=strtok(str," ") strtok用于分割字符串 查一下！！！

char *args=cmd+strlen(cmd)+1 这一堆是什么意思

5.x 的含义还是不太清楚

6.union和struct 区别是什么

7.因为是对一个字节中位的操作，所以位定义类型都是一个类型；

8.有个问题是为什么超过10条就不会打印了？

9.字符串数组也忘得差不多了 笑死 

const char *name[] = {"eax", "ecx"};

10.pa1-1有两个问题：si 只显示9步 | info r寄存器打印有问题

11.什么是assert(0)?

12.为什么是\\\\+???

13.怎么检查左右括号是否匹配 容器

13.传入参数用int 比较好 用token* 感觉不太好 ，因为这里有+1

14.怎样将后面命名的变量在前面会显示未命名



必做任务1：实现正确的寄存器结构体

补充1：结构体中使用冒号对位操作

表示位域，表示占用一个字节的多少位，是对位操作

**因为是对一个字节中位的操作，所以位定义类型都是一个类型；** 

位域的本质上就是一种结构体类型，不同的是其成员是按二进制位来分配的。

>参考：[C语言结构体中的冒号用法_结构体里的冒号-CSDN博客](https://blog.csdn.net/modi000/article/details/103999484?ops_request_misc=%7B%22request%5Fid%22%3A%22159054496219726869063706%22%2C%22scm%22%3A%2220140713.130102334.pc%5Fblog.%22%7D&request_id=159054496219726869063706&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~blog~first_rank_v2~rank_v25-1-103999484.nonecase&utm_term=结构体)
>
>8位示例：因为是8位，所以用的是uint8_t，8位刚好全用完了。

> 联合体与结构体
>
> **各成员共用一块内存空间，并且同时只有一个成员可以得到这块内存的使用权(对该内存的读写)，各变量共用一个内存首地址。**
>
> 利用联合体的特性 可以简化定义寄存器空间的过程

 int  :0; //这里是说明的第二点，空域。下一个位域b将会从下一个字节开始，位3~7为全0。

![8333aad7cb28087a771084760313f36](C:\Users\yiyiyyi\Documents\WeChat Files\wxid_b6g42p1htsbj12\FileStorage\Temp\8333aad7cb28087a771084760313f36.png)

>  补充 :[C 语言中的数据类型：uint8_t、uint16_t、uint32_t、uint64_t_typedef unsigned char uint8;-CSDN博客](https://blog.csdn.net/ningmoon/article/details/136345260)
>
> 这里需要回忆一下 linux中小端大端的存放方式 
>
> 大端 数据低位放在内存中高地址
>
> 小端 数据低位存放在低地址
>
> 这里用的是小端序
>
> 关于uint8_t\uint_16_t\uint32_t\uint64_t
>
> union {
>
> ​      uint32_t _32; //
>
> ​      uint16_t _16;//所以从低地址开始排 才可以达到编辑低16位的目的
>
> ​      uint8_t _8[2];
>
> ​    } gpr[8]; 
>
> 这几个数据共用一个内存空间

​	**开始实现nemu monitor功能**

![image-20240828104008690](C:\Users\yiyiyyi\AppData\Roaming\Typora\typora-user-images\image-20240828104008690.png)

1.Si [N]

首先要识别键盘输入 使用strtok函数！

>[用人话讲懂strtok函数的理解和使用-CSDN博客](https://blog.csdn.net/2302_80198073/article/details/135729196)

 如何使用strtok函数

char arr1[]="si 10"

char* pc;

for(pc=strtok(arr1," ");pc!=NULL;pc = strtok(NULL," "))

{

printf("%s\n",pc);

}

还要学会sscanf函数嘻嘻 类型转换

2.info r 这里不确定答案是不是对的 会变化 但是这里写在cpu_table 里的内容不知道怎么调整

3.x N expr 打印内存expr开始连续N个4字节的存的值

4.表达式求值 

1)**词法分析：**

首先识别出表达式中的单元（**token**）

> 解决方法：正则表达式（快速）Regular
>
> [正则表达式 – 教程 | 菜鸟教程 (runoob.com)](https://www.runoob.com/regexp/regexp-tutorial.html)
>
> 一种用于模式匹配和搜索文本的工具，常用于**验证字符串和查找字符串**
>
> 包含以下字符 . ^ $ + - ? () [] {} \ 
>
> 语法格式 /pattern/flags pattern 模式 flags 修饰符
>
> 模式：字面值字符（字符本身）| 字符类（用[]包围 匹配方括号内任意一个字符）|元字符（e.g \d \w \s 匹配特定类型字符 如数字、字母、空白字符）| 边界符号 如\b $用于匹配字符串开头结尾或单词边界位置

识别token  struct 中 src成员缓冲区溢出 kiss法则 使用assert(0)

> 什么是assert？安全 debug 对参数 执行过程中溢出 越界都可以进行确认保护
>
> [【C/C++】详解 assert() 断言（什么是assert? assert有什么作用？）_c++ assert-CSDN博客](https://blog.csdn.net/weixin_45031801/article/details/136882008)
>
> assert是一个**宏** 用于在运行时检查一个条件是否为真 如果不满足则运行时将终止程序的执行并输出一条错误信息
>
> assert 宏定义在 <assert.h> 或 <cassert> 头文件中，并接受一个参数，通常为一个语句。
> 如果这个语句的结果为 false，assert 宏就会以**"Assertion failed: , file , line "**的形式显示出错信息，然后使程序崩溃并终止运行。如果该语句的结果为 true，则 assert 宏不做任何操作。
> ·用法
>
> assert(condition) condition 就是要判断的条件
>
> e.g
>
> #include <cassert>
> #include <iostream>
> using namespace std;
>
> int main() {
>     int a = 10, b = 20;
>     assert(a > b);    // 条件不成立，程序会终止
>     cout << a + b << endl;
>     return 0;
> }
>
> < 
>
> 

char *regex 与" +"这是字符串就是用一个指针指向开头

&re[i]与rules[i].regex 的compare 

任务要求 写个什么样的正则表达式可以识别十六进制 十进制数

还得学习下strncpy等函数

需要注意区分**strcpy()**和**strncpy()**两种函数，前者是复制整个字符串，后者是复制前n个字符。**每次要把str清空，不然计算表达式的时候会答案会累加。**

2.**递归求值:**根据表达式的归纳定义进行递归求值

如何对于一个表达式进行计算？

拆括号->找dominant operator，将运算分为两部分，然后再细分（典型分治）->计算

问题在于如何找dominant operator？

1）非运算符的token不考虑

2）出现在括号中的token不是 （整个表达式被括号包围的情况已被排除）

3）dominant operator在整个表达式中优先级最低 **最后一步才执行的运算**

4）有多个优先级最低 根据结合性 最后被结合的是dominant 即靠右的是才是

代码实现思路：
从左到右遍历，将所有符号存储在栈中，然后一个一个出栈，

感觉还可以优化，不用存符号，直接从右到左遍历，有+/-就就确定为op，没有就继续找*/ /

有括号部分直接略过 ，找到)，通过内循环，重置i

根据以上原则，按照上述方法唯一确定dominant operator

Check_parentheses



```c
#include <stdbool.h>

bool check_parentheses(int p, int q) {
    int stack[100];  // 用于模拟栈
    int top = -1;    // 栈顶指针
	for (int i = p; i <= q; i++) {
    if (tokens[i].str == '(') {
        // 遇到左括号，入栈
        if (top < 99) {  // 确保不越界
            stack[++top] = '(';
        }
    } else if (tokens[i].str == ')') 	{
        // 遇到右括号，检查栈
        if (top == -1 || stack[top] 	!= '(') {
            return false;  // 如果没有	匹配的左括号
        }
        top--;  // 出栈
   	 }
	}

	// 检查栈是否为空
	return top == -1;
}
```


![image-20240828234345638](C:\Users\yiyiyyi\AppData\Roaming\Typora\typora-user-images\image-20240828234345638.png)

**不是，还得返回出错标识。。。assert**(0)直接返回 报错终止运行

这里传入参数用int 比较好 用token* 感觉不太好 

选做任务：实现带有负数的算术表达式求值

⚫ 负号和减号都是 - , 如何区分它们?

⚫ 负号是个单目运算符, 分裂的时候需要注意什么?

eval 部分有个bug 如果有A出现就会答案不对



for(;j<=q-p;j++){

​    if(tokens[q-j].type==')'){

​      for(;j<=q-p;j++){

​        if(tokens[q-j].type=='('){

​          break;

​        }

​      }

​    }

​    if(tokens[q-j].type=='+'||tokens[q-j].type=='-'){

​      return q-j;

​    }

  }

  j=0;

  for(;j<=q-p;j++){

​    if(tokens[q-j].type==')'){

​      for(;j<=q-p;j++){

​        if(tokens[q-j].type=='('){

​          break;

​        }

​      }

​    }

​    if(tokens[q-j].type=='*'||tokens[q-j].type=='/'){

​      return q-j;

​    }

  }

这个方法过于暴力，适合于式子较少的情况

bool check_parentheses(int p,int q){

  //principle: leftmost and rightmost should be matched.

  char parenthe[100]={0};

  int num_pare=0;

  for(;p<=q;p++){

​    if(tokens[p].type=='('||tokens[p].type==')'){

​      num_pare++;

​      parenthe[num_pare]=tokens[p].type;

​      

​    }

  }

  int j=num_pare;

  int k=1;

  while(1){

​    if(j==k||j==0){

​      break;

​    }

​    if(parenthe[j]==')'&&parenthe[k]=='('){

​      j--;

​      k++;

​    }else{

​      return false;

​    }

​    }

​    return true;

  }

  自己写的check不知道为什么过不了



PA 1-3

> 内存池管理
>
> [【总结笔记】深度理解内存池技术 - MasterBean - 博客园 (cnblogs.com)](https://www.cnblogs.com/MasterBea
>
> 链表的使用
>
> [【数据结构】链表(单链表实现+详解+原码)-CSDN博客](https://blog.csdn.net/Edward_Asia/article/details/120876314)

**思考题2：框架代码中定义 wp_pool 等变量的时候使用了关键字 static，static 在此处的含义是什么? 为什么要在此处使用它?**



这里犯了一个错误 监视点不是看内存 而是看那个式子的值的变化情况 所以要保存原式和其exec上一步的结果再来比较 

有点不懂为什么在check的时候要输出eip 乐



原来rule中放置的先后顺序会影响识别规则的先后应用

strtok(NULL," ")和strtok(args," ")有什么区别



乐，居然把%eax打出来了 笑的相似

/*if(tokens[p].type=='!'){

​      sscanf(tokens[q].str,"%d",&result);//要是这里后面是括号怎么办呢？

​      return !result;

​    }else if(tokens[p].type==REG){

​      if (!strcmp(tokens[p].str, "$eax")){

​          result = cpu.eax;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$ecx")){

​          result = cpu.ecx;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$edx")){

​          result = cpu.edx;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$ebx")){

​          result = cpu.ebx;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$esp")){

​          result = cpu.esp;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$ebp")){

​          result = cpu.ebp;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$esi")){

​          result = cpu.esi;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$edi")){

​          result = cpu.edi;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$eip")){

​          result = cpu.eip;

​          return result;

​        } else {

​          assert(0);

​          return 0;

​        }

​    }*/

​    乐 一段代码好几个bug不知道怎么输出的正确答案

bool check_(){

  bool check=false;

  bool *success=false;

  WP* tmp=head;

  int expr_tmp;

  while(tmp!=NULL){

​    expr_tmp=expr(tmp->expr_watching,success);

​    if(expr_tmp!=tmp->value_watching)//这里是没发生改变的情况

​    {

​      check=true;

​      printf("Hint watchpoint %d at address 0x%08x\n",tmp->NO,cpu.eip);

​      tmp=tmp->next;

​      continue;

​    }

​      printf("Watchpoint %d: %s\n",tmp->NO,tmp->expr_watching);

​      printf("Old value = %d\n",tmp->value_watching);

​      printf("New value = %d\n",expr_tmp);

​      tmp->value_watching=expr_tmp;

​      tmp=tmp->next;

  }

  return check;

}

int eval(int p,int q){

  int result=0;

  if(p>q){

​    assert(0);

  }else if(p==q){

​    if(tokens[p].type==NUM){

​    sscanf(tokens[p].str,"%d",&result);

​    return result;

​    }else if(tokens[p].type==HEX){

​      int i=2;

​      while(tokens[p].str[i]!='\0')//从高位到低位

​      {

​        result*=16;

​        result+=tokens[p].str[i]<58?tokens[p].str[i]-'0':tokens[p].str[i]-'a'+10;//这里只默认会只有a出现

​        i++;

​      }

​      return result;

​    }else if(tokens[p].type==REG){

​      if(!strcmp(tokens[p].str,"$eax")){

​        result =cpu.eax;

​        if(cpu.eax==0){

​          return 0;

​        }

​        return result;

​      }else if(!strcmp(tokens[p].str,"$ecx")){

​        result =cpu.ecx;

​        if(cpu.ecx==0){

​          return 0;

​        }

​        return result;

​      }else if(!strcmp(tokens[p].str,"$edx")){

​        result =cpu.edx;

​        if(cpu.edx==0){

​          return 0;

​        }

​        return result;

​      }else if(!strcmp(tokens[p].str,"$ebx")){

​        result =cpu.ebx;

​        if(cpu.ebx==0){

​          return 0;

​        }

​        return result;

​      }else if(!strcmp(tokens[p].str,"$esp")){

​        result =cpu.esp;

​        if(cpu.esp==0){

​          return 0;

​        }

​        return result;

​      }else if(!strcmp(tokens[p].str,"$ebp")){

​        result =cpu.ebp;

​        if(cpu.ebp==0){

​          return 0;

​        }

​        return result;

​      }else if(!strcmp(tokens[p].str,"$esi")){

​        result =cpu.esi;

​        if(cpu.esi==0){

​          return 0;

​        }

​        return result;

​      }else if(!strcmp(tokens[p].str,"$edi")){

​        result =cpu.edi;

​        if(cpu.edi==0){

​          return 0;

​        }

​        return result;

​      }else if(!strcmp(tokens[p].str,"$eip")){

​        result =cpu.eip;

​        if(cpu.eip==0){

​          return 0;

​        }

​        return result;

​      }else{

​        return 0;

​      }

​    }else{

​      assert(0);

​    }

  }

  else if(check_parentheses(p,q)==true){

​    return eval(p+1,q-1);

  }else {

​    int op=domi_position(p,q);

​    //printf("%d\n",op);

​    /*if(op==-1){

​      if(tokens[p].type==POINT){

​        if(!strcmp(tokens[p+2].str,"$eax")){

​          result=swaddr_read(cpu.eax,4);

​          return result;

​        }else if(!strcmp(tokens[p+2].str,"$ecx")){

​          result=swaddr_read(cpu.ecx,4);

​          return result;

​        }else if(!strcmp(tokens[p+2].str,"$edx")){

​          result=swaddr_read(cpu.edx,4);

​          return result;

​        }else if(!strcmp(tokens[p+2].str,"$ebx")){

​          result=swaddr_read(cpu.ebx,4);

​          return result;

​        }else if(!strcmp(tokens[p+2].str,"$esp")){

​          result=swaddr_read(cpu.esp,4);

​          return result;

​        }else if(!strcmp(tokens[p+2].str,"$ebp")){

​          result=swaddr_read(cpu.ebp,4);

​          return result;

​        }else if(!strcmp(tokens[p+2].str,"$esi")){

​          result=swaddr_read(cpu.esi,4);

​          return result;

​        }else if(!strcmp(tokens[p+2].str,"$edi")){

​          result=swaddr_read(cpu.edi,4);

​          return result;

​        }else if(!strcmp(tokens[p+2].str,"$eip")){

​          result=swaddr_read(cpu.eip,4);

​          return result;

​        }

​      }else if(tokens[p].type==NEG){

​        sscanf(tokens[q].str,"%d",&result);

​        return -result;

​      

​    }else if(tokens[p].type=='!'){

​      sscanf(tokens[q].str,"%d",&result);

​      return !result;

​    }else if (tokens[p].type ==REG) {

​        if (!strcmp(tokens[p].str, "$eax")){

​          result = cpu.eax;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$ecx")){

​          result = cpu.ecx;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$edx")){

​          result = cpu.edx;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$ebx")){

​          result = cpu.ebx;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$esp")){

​          result = cpu.esp;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$ebp")){

​          result = cpu.ebp;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$esi")){

​          result = cpu.esi;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$edi")){

​          result = cpu.edi;

​          return result;

​        } else if (!strcmp(tokens[p].str, "$eip")){

​          result = cpu.eip;

​          return result;

​        } else {

​          assert(0);

​          return 0;

​        }

​      }

​    }*/

​    if(tokens[p].type=='!'){

​      sscanf(tokens[q].str,"%d",&result);

​      return !result;

​    }

​    int val1=eval(p,op-1);

​    int val2=eval(op+1,q);

​    switch(tokens[op].type){

​      case '+': return val1+val2;

​      case '-': return val1-val2;

​      case '/': return val1/val2;

​      case '*': return val1*val2;

​      case OR: return val1||val2;

​      case AND:return val1&&val2;

​      case EQ: 

​        if(val1==val2) return 1;

​        else return 0;

​      case NOTEQ:

​        if(val1==val2) return 0;

​        else return 1;

​      default:assert(0);

​    }

  }

  return 0;

}

修改了初始化处expr_watching的值

问题应该在于无法正确使用expr处的值

static WP* new_wp(){

  //free_  闲置列表 head 使用列表

  WP* tmp1=free_ ;//从尾部取出闲置

  free_=free_->next;//free链表指向新的闲置

  tmp1->next=NULL;

  if(head==NULL){

​    //head为空 那么直接插入即可

​    head=tmp1;

  }else{

​    //否则需要遍历找到尾部 插入

​    //需要注意一点的是 并不是用i去遍历（这是物理上的） 

​    //而是在链表的逻辑上next遍历

​    WP* tmp2;

​    tmp2=head;//遍历起点

​    while(tmp2->next!=NULL){

​      tmp2=tmp2->next;

​    }

​    tmp2->next=tmp1;//此时tmp2是head使用列表的最后一个 在这里尾插新的链表

  }

  return tmp1;//这里以及成功插入head 返回新插入的可使用列表

  /*for(i=0;i<NR_WP;i++){

​    if(wp_pool[i].addr_watching==0){

​      return wp_pool[i];

​    }

​    assert(0);

  }

  */

}

//这个就更是考验基本的链表清空操作了

//分情况讨论 

static void free_wp(WP* wp){

  if(head==NULL){

​    assert(0);

  }else if(head==wp){

​    head=head->next;

  }else{

​    //先找到wp 找到指向wp的节点

​    WP* tmp=head;

​    while(tmp->next!=wp&&tmp!=NULL){

​      tmp=tmp->next;

​    }

​    //此时tmp下一个是wp

​    tmp->next=wp->next;

  }

  //这里用头插法插到free_中

  wp->next=free_;

  free_=wp;//之前的头成功变为wp的下一个

  wp->expr_watching[0]='\0';

  wp->value_watching=0;

}

bool check_(){

  bool check = false;

  bool *success = false;

  WP *temp = head;

  int expr_temp=0;

  while(temp != NULL){

​    //printf("%s\n",temp->expr_watching);

​    expr_temp = expr(temp->expr_watching, success);

​    if (expr_temp != temp->value_watching){

​      check = true;

​      printf ("Hint watchpoint %d at address 0x%08x\n", temp->NO, cpu.eip);

​      temp = temp->next;

​      continue;

​    }

​    printf ("Watchpoint %d: %s\n",temp->NO,temp->expr_watching);

​    printf ("Old value = %d\n",temp->value_watching);

​    printf ("New value = %d\n",expr_temp);

​    temp->value_watching = expr_temp;

​    temp = temp->next;

  }

  return check;

}
